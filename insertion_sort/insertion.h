#ifndef INSERTION_H
#define INSERTION_H

/* to simplify the question, we use int as the type of all the 
 * items.
 */
void insertion(int array[], int n);
void print_array(int array[], int n);

#endif /* INSERTION_H */
