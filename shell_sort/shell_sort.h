#ifndef SHELL_SORT_H
#define SHELL_SORT_H

#include <stdio.h>

/* to simplify the problem, we assume that all the items to be sort has a 
 * type of int.
 */
void shell_sort(int array[], int n);
void print_array(int array[], int n);

#endif /* SHELL_SORT_H */
